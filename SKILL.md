---
name: tagging-ecommerce-competitors
description: Use when 需要分析或编辑包含本品与竞品IPV、转化率、成交笔数、付费点击、PPC、ROI或付费成交的电商表格，尤其适用于为每个竞品生成一个互斥标签，以支持筛选、优先级判断和运营决策。
---

# 电商竞品标签

## 核心原则

为每个竞品分配且只分配一个稳定标签。对区间数据进行保守比较，让标签能够直接指导电商运营决策。

**必须使用的子 Skill：** 读取或编辑 `.xlsx`、`.xls`、`.csv`、`.tsv` 文件时，使用 `spreadsheets:Spreadsheets`。

## 工作流程

1. 根据表头名称识别字段，不要依赖固定列号。识别商品ID、IPV/整体点击、转化率、成交笔数、付费点击、PPC、ROI和付费成交笔数。
2. 识别每组本品基准。将IPV、转化率和成交笔数均为精确数值的行视为本品；其后的区间数据行均与该本品比较，直到遇到下一行本品。
3. 解析 `1000~2500`、`2.5万~5万`、`5%~7.5%` 等区间，保留缺失值，不擅自补数。
4. 从上到下执行下方规则，命中第一条后立即停止。
5. 在原表最后新增一列 `核心标签`，并把该列纳入筛选范围。保留原始数据、公式与格式。
6. 输出各标签数量，并说明会影响判断的数据质量问题。

## 互斥标签规则

每个竞品必须且只能命中一个标签；从上到下判断，命中后立即停止。

| 顺序 | 核心标签 | 命中条件 | 为什么关注 | 主要运营动作 |
|---:|---|---|---|---|
| 1 | 数据不足 | 未找到商品，或有效指标不超过1个 | 避免基于残缺数据误判 | 补采数据，不调整预算 |
| 2 | 量优投优 | 竞品整体成交区间下限大于本品成交，且ROI高于本品 | 成交规模和投放效率同时领先 | 最高优先级拆解商品力、客单价、素材和人群 |
| 3 | 量优 | 竞品整体成交区间下限大于本品成交，但未命中“头部强竞品” | 市场成交规模已经形成威胁 | 研究自然排名、价格、评价、活动和流量来源 |
| 4 | 平级竞争 | 整体成交区间可能持平或超过本品 | 可能存在竞争关系 | 研究搜索、推荐、评价、销量基础和内容承接 |
| 5 | 一般观察 | 前四条均未命中 | 暂无足够证据证明其构成高优先级威胁 | 定期监控价格、流量、转化和投放变化 |

本品行不参与竞品判断，统一标记为 `本品基准`。

## 字段口径

| 字段 | 用途 |
|---|---|
| 商品ID | 识别本品与竞品 |
| IPV/整体点击 | 判断流量规模，不直接代表效率 |
| 转化率 | 判断详情页和流量承接 |
| 成交笔数 | 判断整体成交规模 |
| 付费点击量 | 判断投放规模，不直接代表效果好坏 |
| PPC | 判断单次付费点击成本 |
| ROI | 判断广告引导成交金额与广告花费的比例，不等同于利润 |
| 付费成交笔数 | 判断广告归因成交规模 |

## 区间判断口径

- `竞品区间下限 > 本品数值`：确定高于本品。
- `竞品区间上限 < 本品数值`：确定低于本品。
- 本品数值落在竞品区间内：存在重叠，只能判断为可能持平或超过，不能写成确定领先。
- 解析 `1000~2500`、`2.5万~5万`、`5%~7.5%` 等格式时，统一转换单位后比较。
- ROI为精确数值时直接比较；写作 `比本品高/比本品低` 时使用该相对关系。

## 示例

本品成交136笔、ROI 3.5、付费成交109笔；竞品成交250～500笔、ROI比本品高、付费成交100～250笔。

结果为 `量优投优`：竞品成交区间下限250大于本品136，且ROI更高。命中后不要再添加 `量优` 或 `平级竞争`。

## 修改规则的方法

1. 先确认新条件是否会被更靠前的规则截获。
2. 保证五个标签互斥，不能让同一竞品同时命中多个标签。
3. 新增标签时明确插入顺序、命中条件和运营动作。
4. 修改后使用至少三个场景复测：数据缺失、区间重叠、多个优势冲突。
5. 检查输出仍只有一列 `核心标签`，且标签列已纳入筛选范围。

## 常见错误

- 在五类标签以外临时发明新标签。
- 在一个单元格中拼接多个标签。
- 把付费点击更多或PPC更高直接理解为表现更好。
- 把与本品重叠的区间写成确定领先。
- 在没有毛利率和退款数据时，把广告引导ROI直接当作利润。
- 修改后产生永远无法命中的规则。
